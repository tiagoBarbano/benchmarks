{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "cwd": "${workspaceFolder}", // <- Define onde procurar o .env
            "envFile": "${workspaceFolder}/.env" // <- Ajuda o VSCode a carregar as variáveis
        }
    ]
}